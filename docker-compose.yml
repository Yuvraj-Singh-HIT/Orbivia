version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./frontend/static/results:/app/frontend/static/results
    environment:
      - FLASK_APP=app.py
      - PORT=5000
    restart: unless-stopped
